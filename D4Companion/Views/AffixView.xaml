<UserControl x:Class="D4Companion.Views.AffixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:D4Companion.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Width="{Binding ActualWidth, RelativeSource = {RelativeSource AncestorType = {x:Type UserControl}}}" 
          Height="{Binding ActualHeight, RelativeSource ={RelativeSource AncestorType = {x:Type UserControl}}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Row="0"
                      Grid.Column="0"
                      Grid.RowSpan="5"
                      MinWidth="230"
                      Margin="10 10 5 10"
                      Padding="0 0 5 0"
                      HorizontalContentAlignment="Stretch"
                      HorizontalScrollBarVisibility="Disabled">

            <StackPanel Margin="0 0 0 10" Orientation="Vertical">
                
            </StackPanel>

        </ScrollViewer>

        <DockPanel Grid.Row="0" Grid.Column="1">

            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0 0 0 5">
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Core" IsChecked="{Binding ToggleCore}"/>
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Barbarian" IsChecked="{Binding ToggleBarbarian}"/>
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Druid" IsChecked="{Binding ToggleDruid}"/>
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Necromancer" IsChecked="{Binding ToggleNecromancer}"/>
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Rogue" IsChecked="{Binding ToggleRogue}"/>
                <ToggleButton MinWidth="50" Margin="0 0 5 0" Content="Sorcerer" IsChecked="{Binding ToggleSorcerer}"/>
            </StackPanel>

            <Grid DockPanel.Dock="Top">
                <TextBox Name="TextBoxFilterAffix" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding AffixTextFilter, UpdateSourceTrigger=PropertyChanged}" Width="auto" HorizontalAlignment="Stretch" GotFocus="TextBoxFilterAffix_GotFocus" LostFocus="TextBoxFilterAffix_LostFocus"/>
                <TextBlock Name="TextBoxFilterAffixWatermark" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Enter filter" Width="auto" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="5" Padding="5 0 0 0" IsHitTestVisible="False" Foreground="DimGray" FontStyle="Italic"/>
            </Grid>

            <TabControl>
                <TabItem Header="Affixes">
                    <ListBox ItemsSource="{Binding AffixesFiltered}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             VerticalContentAlignment="Top">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Margin="5" Width="{Binding ActualWidth, RelativeSource = {RelativeSource AncestorType = {x:Type TabControl}}}" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="LightGray" BorderThickness="1" Width="200" Margin="-5 0 0 0" Padding="5" VerticalAlignment="Top">
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding IdSno}"/>
                                        <TextBlock Text="{Binding IdName}"/>
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </TabItem>
                <TabItem Header="Aspects">
                    <!-- Todo: Aspects -->
                </TabItem>
            </TabControl>

        </DockPanel>
    </Grid>
</UserControl>
